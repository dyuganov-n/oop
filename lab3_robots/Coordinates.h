#pragma once

//using namespace std;

struct Coordinates {
	ptrdiff_t x = 0;
	ptrdiff_t y = 0;
};

/*
bool operator==(const Coordinates& l, const Coordinates& r) {
	if (l.x == r.x) {
		if (l.y == r.y) {
			return true;
		}
	}
	else {
		return false;
	}
}
*/